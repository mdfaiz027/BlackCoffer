<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Define the default color -->
    <item android:color="@color/chip_default_color" />

    <!-- Define the pressed color -->
    <item android:color="@color/chip_pressed_color" android:state_pressed="true" />

    <!-- Define the selected color -->
    <item android:color="@color/chip_selected_color" android:state_selected="true" />
</selector>
